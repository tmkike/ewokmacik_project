apiVersion: v1
kind: Service
metadata:
  name: loan-mongodb
spec:
  selector:
    app: loan-mongodb
  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
