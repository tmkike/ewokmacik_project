apiVersion: v1
kind: Service
metadata:
  name: book-mongodb
spec:
  selector:
    app: book-mongodb
  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
